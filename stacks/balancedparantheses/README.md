### Description

Given a string expression, examine whether the pairs and the orders of `{`,`}`,`(`,`)`,`[`,`]` are correct

### Example 1:

```
Input: “[()]{}{[()()]()}”
Output: true
```

### Example 2:

```
Input: “[(])”
Output: false
```